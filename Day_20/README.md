# Day 20 — File I/O + Self JOIN

> **Month 1 — Foundations · Week 3**

---

## 🐍 Python
open(file, 'r'/'w'/'a'). read(), readlines(), write(). with statement (auto-closes). Reading CSV manually.

## 🗄️ SQL
Self JOIN: join a table to itself. Example: employees and their managers in same table. Use aliases.

## 🎨 Frontend (HTML / CSS / JS)
Media queries: @media (max-width: 768px). Mobile-first approach. Common breakpoints: 480, 768, 1024px.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
