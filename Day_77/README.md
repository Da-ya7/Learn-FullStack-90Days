# Day 77 — Gunicorn + Railway deploy

> **Month 3 — Production · Week 12**

---

## 🐍 Python
pip install gunicorn. gunicorn myproject.wsgi:application --workers 3. Procfile: web: gunicorn myproject.wsgi.

## 🗄️ SQL
Advanced JOIN patterns: CROSS JOIN for generating combinations. NATURAL JOIN (avoid in prod). Non-equi JOINs.

## 🎨 Frontend (HTML / CSS / JS)
Portfolio final pass: ensure all pages responsive, all API calls handle errors, loading states everywhere. Cross-browser test.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
