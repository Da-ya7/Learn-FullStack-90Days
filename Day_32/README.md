# Day 32 — collections + Recursive CTEs

> **Month 2 — Building · Week 5**

---

## 🐍 Python
Counter(list) for frequency count. defaultdict(list). OrderedDict. namedtuple for readable tuples.

## 🗄️ SQL
Recursive CTEs: find all hierarchy levels (manager → employee → sub-employee). WITH RECURSIVE syntax.

## 🎨 Frontend (HTML / CSS / JS)
JS data types: string, number, boolean, null, undefined, symbol, BigInt. Type coercion gotchas.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
