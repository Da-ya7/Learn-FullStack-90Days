# Day 59 — Django: Forms + ModelForms

> **Month 2 — Building · Week 9**

---

## 🐍 Python
class PostForm(forms.Form) vs class PostForm(forms.ModelForm). is_valid(), cleaned_data. CSRF protection with {% csrf_token %}.

## 🗄️ SQL
Normalization exercise: denormalize a bad schema (repeating groups) into proper 3NF. Document before/after.

## 🎨 Frontend (HTML / CSS / JS)
Build: search/filter UI — input field filters a displayed list in real-time using JS array filter(). No API call.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
