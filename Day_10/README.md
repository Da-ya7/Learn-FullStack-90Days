# Day 10 — Dict methods + Aggregates

> **Month 1 — Foundations · Week 2**

---

## 🐍 Python
Dict methods: keys(), values(), items(), update(), setdefault(), pop(). Nested dicts. Dict from two lists zip().

## 🗄️ SQL
COUNT(*), SUM(salary), AVG(age), MIN(score), MAX(score). COUNT(column) ignores NULLs. COUNT(*) doesn't.

## 🎨 Frontend (HTML / CSS / JS)
div vs span. Class vs id. Multiple classes on one element. CSS: link stylesheet in head, external .css file.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
