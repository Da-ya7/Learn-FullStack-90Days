# Day 33 — Context managers + Views

> **Month 2 — Building · Week 5**

---

## 🐍 Python
with statement in depth. Custom context manager: __enter__ and __exit__. @contextmanager from contextlib.

## 🗄️ SQL
CREATE VIEW v_active_students AS SELECT... . Views simplify complex queries. DROP VIEW. Views are not stored data.

## 🎨 Frontend (HTML / CSS / JS)
JS functions: declaration, expression, arrow functions. Hoisting. Default params. Rest params (...args).

---

*Part of [Learn-FullStack-90Days](../../README.md)*
