# Day 29 — Decorators + RANK functions

> **Month 1 — Foundations · Week 4**

---

## 🐍 Python
@decorator syntax. Closure-based decorator. functools.wraps(). Chaining multiple decorators. @timer example.

## 🗄️ SQL
RANK() vs DENSE_RANK() vs ROW_NUMBER() — difference with tied values. OVER(PARTITION BY dept ORDER BY salary).

## 🎨 Frontend (HTML / CSS / JS)
Build: responsive 3-column card grid with CSS Grid. Cards have image placeholder, title, text, button.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
