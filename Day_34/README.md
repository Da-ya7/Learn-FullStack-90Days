# Day 34 — Abstract classes + Indexes deep dive

> **Month 2 — Building · Week 5**

---

## 🐍 Python
from abc import ABC, abstractmethod. Abstract base classes — enforce interface. Can't instantiate ABC directly.

## 🗄️ SQL
B-tree indexes. Composite indexes (column order matters). Covering index. When NOT to index. SHOW INDEX FROM table.

## 🎨 Frontend (HTML / CSS / JS)
JS arrays: push, pop, shift, unshift, splice, slice, concat, indexOf, includes, join, flat. Array destructuring.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
