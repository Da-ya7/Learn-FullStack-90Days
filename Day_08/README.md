# Day 08 — Sets + DELETE

> **Month 1 — Foundations · Week 2**

---

## 🐍 Python
Sets: add(), remove(), discard(), union(), intersection(), difference(), symmetric_difference(). No duplicates.

## 🗄️ SQL
DELETE FROM: with WHERE. TRUNCATE TABLE vs DELETE. Difference: truncate resets auto-increment, no rollback.

## 🎨 Frontend (HTML / CSS / JS)
Form attributes: placeholder, required, disabled, readonly, min, max, maxlength, pattern attribute.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
