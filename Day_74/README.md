# Day 74 — Django: Caching

> **Month 3 — Production · Week 12**

---

## 🐍 Python
cache.set('key', value, timeout=300). cache.get('key'). @cache_page(60*15) decorator on views. Cache backends: LocMemCache.

## 🗄️ SQL
Data modeling: design schema for a social media app. Users, posts, likes, comments, follows. Write all tables + constraints.

## 🎨 Frontend (HTML / CSS / JS)
Build: infinite scroll — detect when user near bottom (scroll event). Fetch next page. Append to existing list.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
