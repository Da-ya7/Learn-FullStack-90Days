# Day 21 — Modules + Subqueries

> **Month 1 — Foundations · Week 3**

---

## 🐍 Python
import module, from module import func, import as alias. Creating your own module. __name__ == '__main__'.

## 🗄️ SQL
Subquery in WHERE: SELECT * FROM students WHERE age > (SELECT AVG(age) FROM students).

## 🎨 Frontend (HTML / CSS / JS)
CSS variables: --primary-color: #3b82f6; usage: color: var(--primary-color). :root scope. Theming.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
