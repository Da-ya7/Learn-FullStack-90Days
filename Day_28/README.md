# Day 28 — Generators + Window functions intro

> **Month 1 — Foundations · Week 4**

---

## 🐍 Python
yield keyword. Generator functions return values lazily. Generator expressions (x for x in range()). next().

## 🗄️ SQL
Window functions: OVER() clause. ROW_NUMBER() OVER(ORDER BY salary DESC). No need for GROUP BY.

## 🎨 Frontend (HTML / CSS / JS)
CSS Grid: grid-column: 1/3, grid-row, grid-area, named grid areas. Grid vs Flexbox — when to use each.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
