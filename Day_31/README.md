# Day 31 — itertools + CTEs

> **Month 2 — Building · Week 5**

---

## 🐍 Python
itertools: chain(), islice(), product(), combinations(), permutations(), cycle(). Useful for interview problems.

## 🗄️ SQL
CTEs (Common Table Expressions): WITH cte AS (SELECT...) SELECT * FROM cte. Cleaner than nested subqueries.

## 🎨 Frontend (HTML / CSS / JS)
JavaScript intro: script tag (bottom of body), strict mode, console.log(), typeof, var vs let vs const.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
