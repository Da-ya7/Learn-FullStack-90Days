# Day 72 — Django: Custom management commands

> **Month 3 — Production · Week 11**

---

## 🐍 Python
class Command(BaseCommand): add_arguments(), handle(). python manage.py mycommand --arg. Use for scripts, seeding data.

## 🗄️ SQL
Slow query log: SET GLOBAL slow_query_log=ON. long_query_time=1. Read log. Optimize top 3 slowest queries.

## 🎨 Frontend (HTML / CSS / JS)
Responsive dashboard: CSS Grid sidebar + main. Sidebar collapses to top nav on mobile. Content fills remaining space.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
