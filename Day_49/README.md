# Day 49 — Django: apps + INSTALLED_APPS

> **Month 2 — Building · Week 8**

---

## 🐍 Python
python manage.py startapp blog. Add 'blog' to INSTALLED_APPS. App structure: models.py, views.py, urls.py, admin.py.

## 🗄️ SQL
Complex reporting: sales by month using DATE_FORMAT + GROUP BY. Running total using window function SUM() OVER.

## 🎨 Frontend (HTML / CSS / JS)
Promises: new Promise((resolve, reject) => {}). .then().catch().finally(). Promise.all(). Promise.race().

---

*Part of [Learn-FullStack-90Days](../../README.md)*
