# Day 86 — Interview prep: Django + DRF

> **Month 3 — Production · Week 14**

---

## 🐍 Python
Top 20 Django Qs: ORM vs raw SQL, N+1 problem and solution, Django signals use cases, middleware order, request lifecycle.

## 🗄️ SQL
Advanced: triangular JOINs, gaps and islands (consecutive date ranges), pivot table with CASE WHEN + GROUP BY.

## 🎨 Frontend (HTML / CSS / JS)
JS interview: this keyword in different contexts. call(), apply(), bind(). Arrow functions don't have own this. Prototype chain.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
