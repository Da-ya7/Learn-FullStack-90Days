# Day 43 — Logging module + Django ORM setup

> **Month 2 — Building · Week 7**

---

## 🐍 Python
logging.basicConfig(level=INFO). Logger, Handler, Formatter. Log levels: DEBUG, INFO, WARNING, ERROR, CRITICAL.

## 🗄️ SQL
Configure MySQL in Django: DATABASES = {'ENGINE':'django.db.backends.mysql', 'NAME':'db', 'USER':'root'}. pip install mysqlclient.

## 🎨 Frontend (HTML / CSS / JS)
Event delegation: one listener on parent catches children clicks. event.target.matches('.class'). Why it's efficient.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
