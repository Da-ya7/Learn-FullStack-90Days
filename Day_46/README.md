# Day 46 — pytest + ORM relationships

> **Month 2 — Building · Week 7**

---

## 🐍 Python
pytest: test_*.py files. assert statements. @pytest.fixture. @pytest.mark.parametrize. conftest.py. Run: pytest -v.

## 🗄️ SQL
ORM relationships: ForeignKey, ManyToManyField, OneToOneField. .related_set.all(). select_related() to avoid N+1.

## 🎨 Frontend (HTML / CSS / JS)
async/await with Fetch: async function loadData(){ const res = await fetch(url); const data = await res.json(); }

---

*Part of [Learn-FullStack-90Days](../../README.md)*
