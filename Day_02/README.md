# Day 02 — Data types + SELECT basics

> **Month 1 — Foundations · Week 1**

---

## 🐍 Python
Data types: int, float, str, bool, type(). Type conversion: int(), str(), float(). Input() from user.

## 🗄️ SQL
Data types: INT, VARCHAR, DATE, BOOLEAN, TEXT, DECIMAL. CREATE TABLE students (id INT, name VARCHAR(50), age INT);

## 🎨 Frontend (HTML / CSS / JS)
Headings h1–h6, paragraphs p, line break br, horizontal rule hr, strong, em, HTML comments.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
