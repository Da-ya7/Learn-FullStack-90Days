# Day 24 — Class vs instance vars + String functions

> **Month 1 — Foundations · Week 4**

---

## 🐍 Python
Class variables (shared) vs instance variables (unique per object). @classmethod, @staticmethod decorators.

## 🗄️ SQL
String functions: CONCAT(a,b), LENGTH(str), UPPER(), LOWER(), TRIM(), SUBSTRING(str,1,3), REPLACE().

## 🎨 Frontend (HTML / CSS / JS)
CSS transitions: transition: property duration easing. hover effects. transition: all 0.3s ease. transform basics.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
