# Day 55 — Django: Models — field types

> **Month 2 — Building · Week 9**

---

## 🐍 Python
CharField(max_length=), TextField(), IntegerField(), DateTimeField(auto_now_add=True), BooleanField(), ForeignKey().

## 🗄️ SQL
Advanced indexes: partial indexes, functional indexes. Index on expression. Why (city, age) index serves WHERE city= queries.

## 🎨 Frontend (HTML / CSS / JS)
Build: form with client-side JS validation — required fields, email format, password strength. Show inline error messages.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
