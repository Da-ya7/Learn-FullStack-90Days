# Day 73 — Django: Middleware

> **Month 3 — Production · Week 12**

---

## 🐍 Python
Custom middleware class: __init__(get_response), __call__(request). Process request pass to view process response.

## 🗄️ SQL
Database connection pooling: why opening new connection per request is slow. Django default: one connection per thread. django-db-pool.

## 🎨 Frontend (HTML / CSS / JS)
Build: modal dialog — pure CSS + JS. Overlay backdrop. Close on outside click. Close on ESC key. Accessible focus trap.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
