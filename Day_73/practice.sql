-- Day 73 — SQL Practice
-- Topic: Database connection pooling: why opening new connection per request is slow. Django default: one connection per thread. django-db-pool.

-- Write your queries below:

