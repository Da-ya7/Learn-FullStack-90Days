# Day 61 — DRF: Setup + first serializer

> **Month 3 — Production · Week 10**

---

## 🐍 Python
pip install djangorestframework. Add 'rest_framework' to INSTALLED_APPS. First ModelSerializer. SerializerMethodField.

## 🗄️ SQL
Advanced window: PERCENT_RANK() — percentile of row. CUME_DIST() — cumulative distribution. NTILE(4) — quartiles.

## 🎨 Frontend (HTML / CSS / JS)
CORS setup: pip install django-cors-headers. CORS_ALLOWED_ORIGINS. Why CORS exists. Test Fetch from different port.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
