# Day 40 — CSV module + 3NF

> **Month 2 — Building · Week 6**

---

## 🐍 Python
csv.reader(), csv.DictReader(), csv.writer(), csv.DictWriter(). quoting, delimiter. Reading real CSV data.

## 🗄️ SQL
3NF: no transitive dependency (non-key col depends on another non-key col). Most production DBs target 3NF.

## 🎨 Frontend (HTML / CSS / JS)
classList: add(), remove(), toggle(), contains(), replace(). Dynamically changing styles vs classes.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
