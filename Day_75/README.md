# Day 75 — Environment variables + .env

> **Month 3 — Production · Week 12**

---

## 🐍 Python
pip install python-decouple. config('SECRET_KEY'), config('DEBUG', default=False, cast=bool). .env file. Never commit .env.

## 🗄️ SQL
Complex reporting: cohort retention — users who signed up in month X, how many active in month X+1, X+2.

## 🎨 Frontend (HTML / CSS / JS)
Build: real-time search — debounce input (setTimeout trick). Fetch filtered results from Django API as user types.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
