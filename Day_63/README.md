# Day 63 — DRF: APIView

> **Month 3 — Production · Week 10**

---

## 🐍 Python
class PostList(APIView): get(self,request), post(self,request). request.data, Response(), status.HTTP_201_CREATED.

## 🗄️ SQL
Query optimization: avoid SELECT *, use specific columns. Push filters early. Avoid functions on indexed columns in WHERE.

## 🎨 Frontend (HTML / CSS / JS)
Fetch POST: submit form data to Django API. JSON.stringify(). Content-Type: application/json header. Show response.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
