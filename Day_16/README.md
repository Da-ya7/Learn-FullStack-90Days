# Day 16 — *args **kwargs + FOREIGN KEY

> **Month 1 — Foundations · Week 3**

---

## 🐍 Python
*args: variable positional args (tuple). **kwargs: variable keyword args (dict). Combining all parameter types.

## 🗄️ SQL
FOREIGN KEY: CREATE TABLE orders (id INT, student_id INT, FOREIGN KEY (student_id) REFERENCES students(id)).

## 🎨 Frontend (HTML / CSS / JS)
Position: static (default), relative (offsets self), absolute (relative to parent), fixed (viewport), sticky.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
