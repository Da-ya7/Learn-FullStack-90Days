# Day 26 — Encapsulation + CASE WHEN

> **Month 1 — Foundations · Week 4**

---

## 🐍 Python
_ prefix (convention private). __ prefix (name mangling). @property getter. @property.setter. Validation in setter.

## 🗄️ SQL
CASE WHEN score >= 90 THEN 'A' WHEN score >= 80 THEN 'B' ELSE 'C' END AS grade — conditional column.

## 🎨 Frontend (HTML / CSS / JS)
CSS selectors: descendant (a b), child (a > b), adjacent sibling (a + b), general sibling (a ~ b).

---

*Part of [Learn-FullStack-90Days](../../README.md)*
