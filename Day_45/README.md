# Day 45 — asyncio + ORM annotate/aggregate

> **Month 2 — Building · Week 7**

---

## 🐍 Python
async def, await. asyncio.run(). asyncio.gather() for concurrent tasks. create_task(). When to use async vs threading.

## 🗄️ SQL
ORM: .annotate(count=Count('orders')), .aggregate(avg=Avg('score')), .values('city').annotate(). .values_list().

## 🎨 Frontend (HTML / CSS / JS)
Fetch API: fetch(url).then(r => r.json()).then(data => ...).catch(err => ...). GET request to public API.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
