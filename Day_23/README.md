# Day 23 — OOP classes + UNION

> **Month 1 — Foundations · Week 4**

---

## 🐍 Python
class MyClass: __init__(self), self.attribute, instance methods. Creating objects: obj = MyClass(). dir(obj).

## 🗄️ SQL
UNION: combines results of two SELECTs (removes duplicates). UNION ALL: keeps duplicates. Same columns required.

## 🎨 Frontend (HTML / CSS / JS)
Pseudo-elements: ::before, ::after (content property required). ::placeholder, ::selection, ::first-line.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
