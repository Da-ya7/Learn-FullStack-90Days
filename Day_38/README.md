# Day 38 — Regex + Normalization 1NF

> **Month 2 — Building · Week 6**

---

## 🐍 Python
re module: compile(), match(), search(), findall(), finditer(), group(), groups(), sub(), split(). Common patterns.

## 🗄️ SQL
1NF: each cell one value, each row unique. Fix: split 'Python,SQL' into separate rows using junction table.

## 🎨 Frontend (HTML / CSS / JS)
DOM: document.getElementById(), querySelector(), querySelectorAll(). NodeList vs HTMLCollection. DOM tree.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
