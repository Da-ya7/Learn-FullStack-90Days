# Day 35 — Dataclasses + EXPLAIN

> **Month 2 — Building · Week 5**

---

## 🐍 Python
@dataclass decorator. fields, post_init, frozen=True, field(default_factory=list). Much cleaner than __init__.

## 🗄️ SQL
EXPLAIN SELECT...: key column shows index used. rows = estimated rows scanned. type: ALL is bad, ref is good.

## 🎨 Frontend (HTML / CSS / JS)
Array HOF: map(), filter(), reduce(), find(), findIndex(), some(), every(), forEach(). Chain them.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
