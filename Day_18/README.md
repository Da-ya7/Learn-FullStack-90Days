# Day 18 — Lambda + LEFT/RIGHT JOIN

> **Month 1 — Foundations · Week 3**

---

## 🐍 Python
lambda x: x*2. map(func,list), filter(func,list), zip(list1,list2). sorted() with key=lambda.

## 🗄️ SQL
LEFT JOIN: all rows from left table even if no match right. RIGHT JOIN: opposite. NULL appears where no match.

## 🎨 Frontend (HTML / CSS / JS)
Flexbox: flex-wrap, gap, flex-grow, flex-shrink, flex-basis, align-self, order. Flex shorthand.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
