# Day 12 — for loops + LIKE operator

> **Month 1 — Foundations · Week 2**

---

## 🐍 Python
for loop with range(start,stop,step), enumerate(), zip(). break, continue, pass, else on for loop.

## 🗄️ SQL
LIKE with wildcards: % (any chars) and _ (one char). WHERE name LIKE 'A%' finds names starting with A.

## 🎨 Frontend (HTML / CSS / JS)
Box model: content, padding, border, margin. box-sizing: border-box. width, height, max-width.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
