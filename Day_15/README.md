# Day 15 — Functions + ALTER TABLE

> **Month 1 — Foundations · Week 3**

---

## 🐍 Python
def functions, parameters, return values, default parameters, calling functions. Docstrings with triple quotes.

## 🗄️ SQL
ALTER TABLE: ADD COLUMN, DROP COLUMN, MODIFY COLUMN (change type/constraint), RENAME COLUMN.

## 🎨 Frontend (HTML / CSS / JS)
Display: block, inline, inline-block, none. When each is used. Hiding elements: display:none vs visibility:hidden.

---

## ⭐ Deliverable
Write 5 Python functions + run 10 SQL queries from scratch — no copy-paste

---

*Part of [Learn-FullStack-90Days](../../README.md)*
