# Day 39 — JSON module + 2NF

> **Month 2 — Building · Week 6**

---

## 🐍 Python
json.loads(str), json.dumps(obj, indent=2). Reading/writing JSON files. Handle non-serializable types with default=.

## 🗄️ SQL
2NF: no partial dependency (non-key column depends on part of composite key). Fix with separate tables.

## 🎨 Frontend (HTML / CSS / JS)
DOM manipulation: innerHTML, textContent, setAttribute, getAttribute, createElement, appendChild, insertBefore, remove.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
