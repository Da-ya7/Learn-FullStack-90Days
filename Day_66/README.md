# Day 66 — DRF: JWT Authentication

> **Month 3 — Production · Week 10**

---

## 🐍 Python
pip install djangorestframework-simplejwt. /api/token/ and /api/token/refresh/ endpoints. ACCESS_TOKEN_LIFETIME in settings.

## 🗄️ SQL
JSON columns MySQL 8: JSON data type. JSON_EXTRACT(data, '$.key'), ->shorthand. JSON_ARRAY(), JSON_OBJECT().

## 🎨 Frontend (HTML / CSS / JS)
Protected pages: check localStorage for token before rendering. Fetch with Authorization: Bearer {token} header.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
