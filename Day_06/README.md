# Day 06 — List methods + ORDER BY

> **Month 1 — Foundations · Week 1**

---

## 🐍 Python
List methods: sort(), sorted(), reverse(), copy(), count(), index(), extend(). List in list (2D).

## 🗄️ SQL
ORDER BY ASC (default) and DESC. LIMIT 10. OFFSET for pagination. LIMIT 5 OFFSET 10 (page 3).

## 🎨 Frontend (HTML / CSS / JS)
Forms: form tag, action, method. Input types: text, email, password, number, date.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
