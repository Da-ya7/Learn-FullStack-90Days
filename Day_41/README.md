# Day 41 — requests library + ER diagrams

> **Month 2 — Building · Week 6**

---

## 🐍 Python
requests.get(url, params={}), requests.post(url, json=data). response.json(), response.status_code, response.headers.

## 🗄️ SQL
ER diagrams: entities (rectangles), attributes (ovals), relationships (diamonds). One-to-many, many-to-many, one-to-one.

## 🎨 Frontend (HTML / CSS / JS)
Events: addEventListener('click', fn), event object, event.target, event.currentTarget, event.preventDefault().

---

*Part of [Learn-FullStack-90Days](../../README.md)*
