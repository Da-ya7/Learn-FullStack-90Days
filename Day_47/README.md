# Day 47 — venv deep dive + ORM advanced

> **Month 2 — Building · Week 8**

---

## 🐍 Python
requirements.txt: pip freeze > requirements.txt. pip install -r requirements.txt. .gitignore the venv folder.

## 🗄️ SQL
ORM advanced: prefetch_related() for M2M. .only() to select specific columns. .defer() to exclude heavy columns.

## 🎨 Frontend (HTML / CSS / JS)
JSON: JSON.parse(string), JSON.stringify(obj, null, 2). Working with nested JSON from APIs.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
