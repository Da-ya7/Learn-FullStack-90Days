# Day 48 — Django: project setup

> **Month 2 — Building · Week 8**

---

## 🐍 Python
django-admin startproject myproject. Folder structure: settings.py, urls.py, wsgi.py, manage.py. python manage.py runserver.

## 🗄️ SQL
ORM raw SQL: Model.objects.raw('SELECT * FROM app_model WHERE id=%s', [id]). connection.cursor() for complex queries.

## 🎨 Frontend (HTML / CSS / JS)
localStorage: setItem('key','val'), getItem('key'), removeItem('key'), clear(). Store/retrieve user preferences.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
