# Day 69 — DRF: Pagination

> **Month 3 — Production · Week 11**

---

## 🐍 Python
PageNumberPagination: page_size=10. LimitOffsetPagination. CursorPagination for large datasets. Custom page size via query param.

## 🗄️ SQL
Prepared statements: PREPARE stmt FROM 'SELECT * FROM users WHERE id = ?'. EXECUTE stmt USING @id. Prevents injection.

## 🎨 Frontend (HTML / CSS / JS)
CRUD UI: Edit button populate form PUT request update item in DOM. Delete button DELETE request remove from DOM.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
