# Day 68 — DRF: Filtering + Search

> **Month 3 — Production · Week 11**

---

## 🐍 Python
pip install django-filter. DjangoFilterBackend, SearchFilter, OrderingFilter. filterset_fields, search_fields, ordering_fields.

## 🗄️ SQL
SQL injection: how it works ('; DROP TABLE). How to prevent: parameterized queries, Django ORM (auto-safe), never .format().

## 🎨 Frontend (HTML / CSS / JS)
CRUD UI: Add button show form POST append new item to list without page reload. Optimistic UI update.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
