# Day 17 — Scope + INNER JOIN

> **Month 1 — Foundations · Week 3**

---

## 🐍 Python
Local vs global scope. global keyword. Nonlocal in nested functions. LEGB rule: Local→Enclosing→Global→Built-in.

## 🗄️ SQL
INNER JOIN: SELECT s.name, o.product FROM students s INNER JOIN orders o ON s.id = o.student_id.

## 🎨 Frontend (HTML / CSS / JS)
Flexbox: display:flex, flex-direction (row/column), justify-content (center/space-between), align-items.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
