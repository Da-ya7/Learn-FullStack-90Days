# Day 44 — Threading + ORM filter/exclude

> **Month 2 — Building · Week 7**

---

## 🐍 Python
threading.Thread(target=fn). start(), join(). daemon=True. Thread safety basics. GIL — what it means for CPU tasks.

## 🗄️ SQL
Django ORM: .filter(age__gt=18), .exclude(city='Delhi'), .filter(name__contains='raj'), Q objects for OR queries.

## 🎨 Frontend (HTML / CSS / JS)
Form events: input, change, submit. Get values with e.target.value. FormData object. Prevent default submit.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
