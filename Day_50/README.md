# Day 50 — Django: URL routing

> **Month 2 — Building · Week 8**

---

## 🐍 Python
urlpatterns = [path('posts/', views.post_list, name='post_list')]. include() for app urls. Dynamic: path('post/<int:pk>/', ...).

## 🗄️ SQL
MySQL user management: CREATE USER 'dev'@'localhost' IDENTIFIED BY 'pwd'. GRANT ALL ON db.* TO 'dev'@'localhost'.

## 🎨 Frontend (HTML / CSS / JS)
ES6 modules: export function, export default, import {fn} from './module.js'. script type='module'.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
