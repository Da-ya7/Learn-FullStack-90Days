# Day 25 — Inheritance + Date functions

> **Month 1 — Foundations · Week 4**

---

## 🐍 Python
class Child(Parent). super().__init__(). Method overriding. isinstance(), issubclass(). Multiple inheritance.

## 🗄️ SQL
Date functions: NOW(), CURDATE(), DATE(datetime), YEAR(), MONTH(), DAY(), DATEDIFF(d1,d2), DATE_ADD().

## 🎨 Frontend (HTML / CSS / JS)
overflow: hidden/scroll/auto/visible. z-index (stacking context). opacity. visibility: hidden vs display:none.

---

*Part of [Learn-FullStack-90Days](../../README.md)*
